apiVersion: v2
name: vault-deploy
description: Helm chart to deploy HashiCorp Vault via GitOps
type: application
version: 0.1.0
appVersion: "1.15.2"

dependencies:
  - name: vault
    version: 0.28.1
    repository: https://helm.releases.hashicorp.com
